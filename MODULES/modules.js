// modules
// commonJS, every file is a module (by default)
// Modules = Encapsulated code (*on;ly share minimum )

const names = require("./names");
const sayHi = require("./utils");
const data = require("./arrays");
require("./grenade");
// sayHi("Susan");
// sayHi(names.peter);
// sayHi(names.john);
